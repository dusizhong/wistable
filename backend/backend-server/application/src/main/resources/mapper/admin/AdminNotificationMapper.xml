<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.apitable.admin.mapper.AdminNotificationMapper">

    <sql id="Base_Column_List">
        id, title, content, url, from_user, status, is_deleted, created_at, updated_at
    </sql>

    <select id="selectAdminNotificationPage"
        resultType="com.apitable.admin.entity.AdminNotificationEntity">
        SELECT
        <include refid="Base_Column_List"/>
        FROM ${tablePrefix}admin_notification
        WHERE is_deleted = 0
        <if test="keyword != null and keyword != ''">
            AND title LIKE CONCAT('%', #{keyword}, '%')
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
